name: Tests

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version number (leave empty for auto)"
        required: false
        type: string
      use_self_hosted_runners:
        description: Use self-hosted runners.
        required: false
        default: true
        type: boolean

  pull_request:
    branches: 
      - main
      - dev
  push:
    branches:
      - main
      - dev
    paths-ignore:
      - "README.md"
      - ".github/**"

env:
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1
  DOTNET_NOLOGO: true

jobs:
  test:
    name: test
    uses: ./.github/workflows/run-tests.yml
    with:
      # Upstream PRs use bitbound self-hosted runners. Fork PRs cannot access
      # those machines and must remain verifiable on GitHub-hosted runners.
      use_self_hosted_runners: ${{ github.repository_owner == 'bitbound' && (github.event_name != 'workflow_dispatch' || inputs.use_self_hosted_runners) }}
